#include <iostream> // preprocessor directive to use input output stream from standar library
using namespace std; // to avoid using std:: again and again

int main(){
  char letter; // variable define to store value of letter
  cin >> letter;
  cout << "Letter: " << letter << endl;
  return 0;
}
